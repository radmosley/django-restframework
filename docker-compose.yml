version: '3'

services:
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env

  django:
    build: .
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      